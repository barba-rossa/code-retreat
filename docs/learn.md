# Learning Log (Historian)

> Add a new section every loop. Keep it short and actionable.

## Loop 1 – (timestamp)
- **Goal:** Make `add("")` return 0; `add("1")` returns 1.
- **Micro‑pattern:** Minimal‑Pass‑Then‑Refactor – ask AI for smallest change only.
- **Pitfall:** AI over‑abstracted; we restated constraint “no refactor”.
- **Reusable prompt:** See `docs/prompts.md#coder-minimal-pass`.
- **Open question:** What edge case should fail next?

